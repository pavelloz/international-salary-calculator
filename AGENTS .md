## Development Commands

The project uses **Bun** as the primary package manager and runtime. Run all commands via `bun`.

- **Start Dev Server**: `bun run start` -- starts server at http://localhost:4321/
- **Build**: `bun run build`
- **Type Check**: `bun run check`
- **Run Tests**: `bun run test`
