## Stack

Interesting, performance oriented stack. It shreds in the speed and weight.

Bun + vitest @ happy-dom + astro + solid-js + nanostores + valibot

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command         | Action                                      |
| :-------------- | :------------------------------------------ |
| `bun i`         | Installs dependencies                       |
| `bun start`     | Starts local dev server at `localhost:4321` |
| `bun run build` | Build your production site to `./dist/`     |
| `bun run test`  | Run tests                                   |
